spring:
  datasource:
    driverClassName: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        temp:
          use_jdbc_metadata_defaults: false
        format_sql: false
dndadventures:
  webapp_url: https://dnd-adventures.herokuapp.com
  secure_token: true
  token_http_only: false
  is_prod: true
